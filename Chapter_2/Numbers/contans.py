"""
A constant is a variable whose value stays the same throughout the life of a
program. Python doesn’t have built-in constant types, but Python programmers
use all capital letters to
"""

"When you want to treat a variable as a constant un your code, write the name of the variable in all capital letters"

MAX_CONNECTIONS = 5000

print(MAX_CONNECTIONS)